{{ ansible_managed | comment }}

[Unit]
Description=Miniflux backup
StartLimitIntervalSec=0

[Service]
Type=oneshot
User=root
ExecStart={{ scriptsdir }}/backup-miniflux.sh
WorkingDirectory={{ backupdir }}

[Install]
WantedBy=multi-user.target
